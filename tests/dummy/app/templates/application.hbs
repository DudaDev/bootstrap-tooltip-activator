<h2 id="title">Welcome to Ember.js</h2>
{{#bootstrap-tooltip-activator}}
	{{#each buttons as |button|}}
		<button
			class='btn btn-default'
			data-toggle="tooltip"
			data-placement="{{button.placement}}"
			data-original-title="{{button.tip}}">
			{{button.name}}
		</button>	
	{{/each}}
{{/bootstrap-tooltip-activator}}
{{outlet}}
